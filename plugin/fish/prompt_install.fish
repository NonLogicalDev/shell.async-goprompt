# PROMPT_FISH_ASYNC: -----------------------------------------------------------
if type -q {$GOPROMPT}
    {$GOPROMPT} install fish.plugin | source
end
# ------------------------------------------------------------------------------